#if !defined(SNAKE_H)
/*=============================================================
Creator: Zachary Lecerf 
Notice: Copyright (C) 2026 Zach Lecerf <zach@dyforge.com>
===============================================================*/
#include "platform_snake.h"

#define g_internal static
#define local_persist static
#define global_variable static

#define Pi32 3.14159265359f

#define Kilobytes(Value) ((Value)*1026LL)
#define Megabytes(Value) (Kilobytes(Value)*1026LL)
#define Gigabytes(Value) (Megabytes(Value)*1026LL)
#define Terabytes(Value) (Gigabytes(Value)*1026LL)

#define ArrayCount(Array) (sizeof(Array) / sizeof(Array)[0]))



#endif
